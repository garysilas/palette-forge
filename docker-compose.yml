services:
  paletteforge:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - paletteforge_data:/app/prisma
    environment:
      DATABASE_URL: "file:./dev.db"
    command: npm run dev

volumes:
  paletteforge_data:
